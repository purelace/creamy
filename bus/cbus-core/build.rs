/*
use std::path::Path;

use message_generator::protocol::generate_code_from_xml;

fn main() -> Result<(), Box<dyn core::error::Error>> {
    /*
      let path = "/run/media/irisu/A4465B46465B187E/fusionwm/fusion/api/sys.xml";
      let content = std::fs::read_to_string(path)?;
      let generated = generate_code_from_xml(content.as_str())?;

      eprintln!("{generated}");

      let out_dir = std::env::var("OUT_DIR").unwrap();
      let dest_path = Path::new(&out_dir).join("sys.rs");

      let file = syn::parse2(generated)?;
      let formatted = prettyplease::unparse(&file);

      std::fs::write(dest_path, formatted)?;

    println!("cargo:rerun-if-changed=build.rs");
    */

    Ok(())
}
 */

fn main() {}
